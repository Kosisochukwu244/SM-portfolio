[build]
  base = "artifacts/portfolio"
  command = "pnpm install --no-frozen-lockfile && pnpm run build:no-check"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PNPM_VERSION = "10"
  npm_config_engine_strict = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200